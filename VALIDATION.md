# Build and validation record

14 September 2026 (earlier checks retained). Xcode 26.6; iOS 26.5 simulators. Deployment target iOS 18.

## Fast area selection — 14 September

- Production profiling found that each selection copied, installed and reopened all 3,136 cartography cells needed by the fixed 15 km horizon. This cost was effectively constant: 1 × 1, 4 × 4 and 8 × 8 terrain selections each took approximately 12.8 seconds on the development Mac. The rectangle calculation itself took approximately 17 ms; no network transfer was involved.
- Saved areas cut from the signed application bundle now reference that immutable cartography source. Terrain, surrounding heightfields, routes and graphs remain owned by the saved area. Imported packs continue to copy and verify their image payloads because they are mutable and untrusted. Loading a bundled reference validates its atlas metadata and forms the known bundle URLs without re-reading and hashing all 6,272 native/preview files.
- The same end-to-end profile now measures **0.491 s** for 1 × 1 cells, **0.500 s** for 4 × 4 and **0.545 s** for 8 × 8, including prepare, install and reopen. The native and preview map metadata, URLs and pixels are unchanged. A real simulator selection entered interactive 3D within the first 0.8-second observation window; a larger saved-area replacement did the same.
- Regression checks passed: packs **77 assertions**, cartography crop/install/reuse/reference/rollback **27 assertions**, extensions **68 assertions**, and real crop coverage **118,281 assertions**. The latter includes all 256 original cells, native terrain samples and source-map pixel/orientation comparisons. The optimized Debug simulator build succeeded and both initial selection and expansion opened in 3D. A saved reference also adopts compatible cartography from a later bundle build, so map-pack refreshes do not strand existing terrain saves.

## Wider surroundings and gentler haze — 14 September

- Daylight follow-up: a texture-free Metal sky triangle renders soft blue daylight, with a broad sun glow and slightly warmer/brighter slope lighting. Haze samples the same camera-oriented sky colour, keeping the edge blend coherent through camera movement. No new terrain or texture allocations are required by the sky.
- Expand area is visible directly on the 3D map. Tapping overview terrain or moving outside detailed coverage reveals an explanatory card; nearing the saved landscape edge shows a distinct coverage-limit explanation. Expansion opens the existing atlas with the viewed/tapped location, current area and route included, without starting a download. A redundant older Add detail here button was removed from the empty route panel; route-specific detail controls remain available.
- Daylight checks: Debug simulator and signed Release builds; area journey **28 assertions**; extension/navigation focus and recovery **43 assertions**. Simulator inspection confirmed the blue sky, persistent expansion button, overview card after a distant tap, and a selection including that location. Unavailable source coverage correctly disabled saving. Physical touch feel and performance remain unmeasured.
- New preparations target a 15 km distant margin instead of 10 km, clipped to real prepared coverage. The nearby margin remains 2 km. Existing combined scene admission and fallback remain in force; no additional terrain levels, source processing or camera-driven terrain loading were introduced.
- The renderer keeps primary and nearby terrain clear when both surrounding bands are loaded. Haze grows smoothly over 12 km beyond that clear region, reaching 65% tint rather than the former 84%. A separate 2 km outer-edge fade is capped independently to each side's available margin, preventing it from fading primary terrain. Single-band scenes keep primary terrain clear. Map sampling is unchanged; the relief shadow is already omitted for scenes with surrounding terrain.
- Atlas reuse now checks saved surrounding coverage as well as primary bounds and spacing. Re-selecting an old rectangle prepares broader available surroundings; opening an existing save directly retains its stored extent with the new atmosphere.
- Checks passed: real horizon crop/install/validation **297 assertions**, atlas journey/reuse **28 assertions**, extension/route recovery **40 assertions**, Debug simulator and signed Release iPad builds, and whitespace check.
- Simulator prepared and opened a 3.43 × 2.94 km primary area at 4 m with a 7.44 × 6.96 km nearby 8 m layer and the full 27.42 × 27.42 km source horizon at 32 m. Saved metadata matched the source's far bounds exactly. Portrait and landscape rendering were inspected; screenshot: `Tools/reports/QA/haze/wider-horizon.png`. Physical performance has not been measured. The signed build was installed on Matthew's M1 iPad Pro; automatic launch was denied because it was locked.

## One-screen atlas area selection — 14 September

- The atlas now supports an explicit rectangle mode: draw or tap opposite corners, resize corner handles, move the rectangle, or use centre/size controls. Browse gestures are separate. The grid is hidden; an approved outline snaps outward to the storage footprint. Dimensions, estimated total saved assets and the save/open action stay on the same screen, with inline preparation progress and cancellation.
- New atlas selections stay at 4 m. Oversized or unavailable rectangles are rejected without silent quality reduction or clipping. A previous admitted footprint remains outlined while an invalid proposal is shown. Exact saved selections reopen directly; saved footprints are shaded. Non-grid crop identities are deterministic so the same rectangle can be reused.
- Expand area opens the same full-screen atlas from 3D. Its initial rectangle contains the existing area and all route points; GPX segment gaps remain separate in the preview. The existing transactional extension preserves the route, history and geographic camera. Missing source coverage remains explicit. There is no hosted nationwide download catalogue, and the adaptive mesh remains experimental; current source-backed actions truthfully say no download is needed.
- Identical cartography payloads reuse immutable files across saved areas using hard links, with verified copy fallback. Mutable source files are never linked. Shared maps survive removal of either owning area; terrain and graph crops remain separate. The displayed saved asset estimate remains a conservative logical total, not incremental physical storage.
- Checks passed: atlas area journey **25 assertions**, extension transaction **40 assertions**, cartography crop/install/reuse/rollback **19 assertions**, and crop suite **118,281 assertions** including real sample and map comparisons. Debug simulator and signed Release device builds succeeded; diff whitespace check passed.
- Simulator: selected an area by opposite corners, inspected source-coverage rejection, saved a 3.4 × 2.0 km rectangle using inline progress, and opened it in 3D with a geographic name. Relaunched, reopened its overview waypoint, opened full-screen expansion, enlarged to 4.4 × 3.9 km, saved and returned to 3D with that point and the camera preserved. Screenshots: `Tools/reports/QA/area-selection/expand-area.png` and `expanded-route.png`. Portrait selection and its fixed save panel were also inspected (`portrait-selection.png`). Automated mouse drags did not reliably exercise continuous simulator touch dragging; corner-drag and multitouch feel still need a hands-on iPad check. The signed Release update was installed on the connected M1 iPad Pro. Automatic launch was rejected because the iPad was locked; it must be opened after unlocking.

## Gesture settings — 14 September

- Settings now opens from both the atlas and the 3D action menu. One finger moves the map by default; two fingers rotate and tilt. The alternate layout restores the previous touch counts. Pinch zoom and double-tap fit remain available.
- The preference uses app-local UserDefaults, applies to the existing gesture recognizers without recreating terrain, and updates visible and accessibility help. The privacy manifest declares the app-local UserDefaults reason CA92.1 ([Apple API reasons](https://developer.apple.com/documentation/bundleresources/app-privacy-configuration/nsprivacyaccessedapitypes/nsprivacyaccessedapitype)).
- Debug simulator and Release iOS builds passed. Simulator checks verified both choices, persistence across process relaunch, opening Settings over a saved route and immediate help updates after switching back; the terrain viewpoint and route stayed in place. Screenshot: `Tools/reports/QA/settings/gesture-settings.png`. The signed Release build was subsequently installed and launched successfully on Matthew’s connected M1 iPad Pro (12.9-inch, 5th generation), iPadOS 26.6.1, using the existing signing configuration. Physical multitouch feel still requires hands-on verification.

## Detailed coverage and overview sketches — 14 September

- The centre-of-view coverage badge, contextual dashed area boundary, overview route dashes and explicit **Add detail here / Add detail along route** flows are implemented in the iOS planner. Map materials are unchanged. Preview margins are 250 m, 500 m, 1 km or 2 km; the current pack loader prepares a rectangle and the UI states that clearly.
- Outside waypoints are saved as manual sketches without opening a proposal or starting a download. Adding detail preserves waypoint identity, manual geometry, route association, camera pose and undo/redo history; it fills elevations without inventing followed paths. Missing elevations suppress complete ascent/walking-time estimates. Existing associated overview sketches can reopen against saved horizon coverage.
- Relevant tests: `test_routes.sh` **135 assertions**, `test_extensions.sh` **68 assertions**, `test_extension_flow.sh` **40 assertions**, and `test_navigation.sh` **125 checks** passed. These cover boundary crossings, persisted sketches and GPX coordinates, view-centred proposals, explicit margin changes, no duplicate waypoint, history preservation, cancellation and failure recovery.
- Final Debug simulator and Release generic iOS builds succeeded with signing disabled. `git diff --check` passed.
- iPad Pro 11-inch (M5) simulator: placed two overview points and inspected the dashed 157 m sketch; opened the explicit preview in portrait and landscape; saved the expanded 3 × 3 area at 4 m; verified two points remain, terrain and camera survive replacement, and the route receives elevations. Relaunched the final build and reopened the saved route successfully. Screenshot: `Tools/reports/QA/coverage/route-after-add-detail.png`.
- This change does not integrate the experimental adaptive mesh format or alter source packs. Physical M1 iPad Pro performance and multitouch behaviour remain unmeasured. Earlier extension interaction records below describe the previous pending-waypoint flow, superseded by this saved-sketch flow.

## 4 m default and initial loading — 14 September

- New selections start at 4 m with Auto opt-in. Tests cover a capable device that could fit 1 m, an import missing 4 m, a fine-only import and insufficient headroom. Saved terrain and route files are not migrated to a different spacing.
- The actual Eryri source allows its full 16 × 16 cells at 4 m under an injected 8 GiB/modern profile: 7.8266 × 7.8394 km, 4,198,401 primary samples, 936,337,440 estimated scene bytes and 722,020,622 estimated saved bytes, with 8 m/32 m horizons. The 6 GiB/Apple profile also admits the complete source with a 16 m/32 m horizon (852,133,300 scene bytes). These use host Metal texture allocation and injected capacity, with no available-memory/thermal pressure; they are not measurements on the user's M1 iPad Pro.
- Preview upload batches reduce GPU submissions for the saved 2,350-cell area from 2,351 to 74. GPU readback checks cover all 35 cells across two batches, row boundaries, a partial final batch, transparent pixels in reused storage, corrupted data and cancellation. Existing sharpness and oblique seam checks still pass.
- Read-only optimized Mac profile of the existing 8 × 5 area: map setup 1.174 s before batching, 0.681 s after. A headless iOS simulator harness of the production renderer measured 10.558 s without compiler optimization before batching versus 1.370 s optimized after batching; pack loads were 2.988 s and 2.881 s respectively. The scene remained at its existing 2 m spacing, so these gains do not include changing it to 4 m. These are individual warm host runs, not a controlled M1 performance guarantee.
- Debug Swift optimization is now `-O`; symbols remain enabled. This improves dense terrain preparation in development builds, with reduced debugger variable visibility and Xcode canvas compatibility. Release settings and signing were not changed.

## Automated checks

- Independent cartography replaces the former primary/near/far raster-density split for current Eryri scenes. All 3,136 native cells have 1024² inner pixels (approximately 0.48 m/pixel), plus four-pixel neighbour gutters; previews derive from identical source pixels. Verified all 6,272 payloads, all native/preview canonical pixel comparisons and 12,320 shared image/preview seams. All 422 pre-existing assets remain byte-identical. Added imagery is 704,072,627 bytes; the complete bundle is 1,288,770,796 bytes.
- `Tools/test_cartography.sh`: **473 assertions passed** for schema and allocation validation, exact row-major bounds, gutters, whole-cell crops, malformed and overflow inputs, all 100 fixture rectangles, map-memory independence across six LiDAR spacings and shared horizon accounting. The allocation-only footprint avoids repeated filename/hash scans; full source/load/install integrity checks remain separate.
- `Tools/test_cartography_renderer.sh`: **288 checks passed** for the bounded cache, visibility, verified uploads, orientation, frame-buffer lifetime, cancellation and the shipping Metal fragment. The high-contrast fixture matches its continuous-image reference with zero channel error at 64, 80 and 96 pixels per cell, including oblique 80 × 24 and 96 × 28 projections; native sampling matches at 512 × 512 and 512 × 160. Actual/reference images are retained in `Tools/reports/QA/atlas-gpu-medium-seam-80-24.png` and `atlas-gpu-medium-seam-96-96.png`.
- Map allocations are independent of LiDAR spacing. Actual GPU allocation checks exposed excessive per-slice/mip padding in the initial preview-array design. Previews now form one continuous mosaic. At the maximum 64 × 64 footprint, measured texture allocations plus decode/metadata reserves total 350.77 MiB on the M4 Pro; device queries estimate 351.00 MiB, below the 384 MiB map cap. The deterministic fallback is 375.44 MiB. Runtime checks both preflight estimates and actual allocations. Combined terrain and map admission is checked separately.
- `Tools/test_cartography_allocation.sh`: **13 actual Metal allocation assertions passed**, covering 1 × 1, 1 × 64, the saved 43 × 43 and 50 × 47 footprints, the 56 × 56 source and the maximum 64 × 64 layout. The harness also reproduces the rejected preview-array design's excessive allocation. Report: `Tests/CartographyAllocationResults.md`.
- Read-only migration checks passed **20 assertions** for the actual saved single-cell and 8 × 5 areas. Both use compatible bundled cartography with unchanged terrain identity and height hashes; all eight saved manifests and the route library remained byte-identical.
- `Tools/test_cartography_medium.sh`: **32 assertions passed** for sequential neighbour decoding, north/south orientation, all corners, shared gutter strips, nonuniform rows, outer coverage edges, integrity rejection, cancellation and exact linear-sRGB reduction.
- `Tools/test_cartography_packs.sh`: **16 assertions passed** for byte-exact native/preview crops, all six terrain spacings, installation, corrupt replacement rollback and automatic map compatibility for older saved areas without rewriting their manifest or terrain identity.
- `Tools/test_atlas_real.sh`: **11,090 assertions passed**. Real original cell (7,8) at 1 m prepares and opens with near 8 m/far 32 m terrain and 1,849 independent map cells. All installed atlas bytes and terrain rows match the sources; legacy map assets are omitted. Actual saved size is 459,889,781 bytes including metadata; estimated scene memory is 478,825,468 bytes (456.6 MiB). Real 4 × 4 and 8 × 5 previews preserve map pixels/files across 1, 2, 4, 8, 16 and 32 m.
- Warm macOS command-line timings for that real save were 2.85 seconds preparation, 4.28 seconds installation and 1.22 seconds loading. Release source-preview timing for a 4 × 4 selection is approximately 10.05 ms, with 0.94 ms for all six allowances. These are host measurements, not iPhone performance guarantees.
- Legacy budget, pack, route, crop and horizon checks continue to exercise the older image-band format explicitly. The extension suite now passes **68 assertions**, including preference for the independent map source over older image bands. The 31-assertion extension transaction suite also passes with the new real atlas source.

- Final Debug simulator build and Release iOS device build both passed with code signing disabled. The only build warning was skipped App Intents metadata extraction because this app has no App Intents dependency.

- `Tools/test_budget.sh`: **342 assertions passed**, including injected RAM/headroom/GPU/buffer/thermal constraints, finest available resolution, stable snapshots, resident-height credit, unavailable Metal zero metrics and invalid huge dimensions. Combined-context cases verify near 8 m → near 16 m → far only → near only → primary only fallback while preserving selected primary spacing and map pixels, and rejection when the primary alone cannot fit.
- `Tools/test_routes.sh`: **124 assertions passed**, including directed graph routing, access restrictions, GPX validation/roundtrips, disconnected tracks, geometry/elevation statistics, route storage, terrain reassociation and empty-route undo/redo persistence.
- `Tools/test_packs.sh`: **77 assertions passed**. Real Snowdon install/load and resolution replacement passed. Cancelled, corrupt, incorrectly dimensioned, all-no-data and unsafe-path payloads leave the previous working installation intact.
- `Tools/test_crops.sh`: **118,280 assertions passed**, including per-row/pixel comparisons across all 256 original cells. Height samples match their source window exactly; map orientation, geographic bounds, clipping and subsequent installation/loading passed. Additional pathless/private-graph crops load with manual planning.
- `Tools/test_horizon.sh`: **245 assertions passed** using real prepared surroundings. Verified 2 km/10 km margins, coverage clipping, whole-area identity, exact cropped sample rows, context maps/hashes, unchanged primary bounds and geometry, atomic installation/loading, primary-only elevation inspection and invalid/corrupt context rejection preserving a previous valid installation.
- `Tools/test_extensions.sh`: **68 assertions passed** for original-grid rectangles, destination padding, exact edges, retaining the entire current area and actual route geometry, local-source coverage and unavailable/invalid inputs. Source selection prefers the independent atlas and complete terrain choices over an older saved crop.
- `Tools/test_extension_flow.sh`: **31 assertions passed** against the real Eryri source. A replacement waits for the old terrain view to release before loading, preserves route identity, geometry, undo/redo and geographic camera pose, then adds the pending waypoint once. Cancellation and unavailable source bytes restore the old saved area and proposal; the original installation remains intact. An earliest-interactive-publication check catches a catalogue-refresh race that previously allowed another edit before the pending waypoint started; a temporary copy with the prior ordering fails this regression check.
- `Tools/test_navigation.sh`: **125 assertions passed** for geographic camera conversion/restoration, primary/context bounds, invalid inputs and exact-edge pick canonicalization. A temporary renderer harness also passed 280 ray comparisons across 20 mixed-resolution, map-seam and NoData scenarios, with cancellation and overlay-gap checks. Production picking traverses bounded grid cells instead of scanning every triangle.
- Fixed-grid checks: all 256 source cells have distinct original IDs and all six terrain levels. Verified exact native windows and north-oriented map crops; 1 m and 2 m crops preserve identical map pixels. A single cell prepares 513 × 513 height samples and a 1024 × 1024 texture. Resolution admission is now tested with injected device contexts: basic contexts retain the earlier small-scene behavior, while capable contexts allow larger native 1 m selections. Physical RAM, process headroom, GPU working-set/buffer limits and thermal state are checked independently.
- A temporary Swift harness exercised the route-selection helper against the real Eryri manifest: 12 checks passed for preserved saved selections, minimal containing rectangles, exact edges, single-point/straight routes and invalid/empty/outside fallbacks.
- Legacy fine-map validation rejects unsafe, incomplete or overlapping layers. A legacy whole-area installation keeps the overview layer and discards unused fine-map metadata; current atlas installations omit both legacy alternatives.
- Direct-buffer mesh construction passed 30 temporary equivalence cases against the previous array algorithm, including exact vertex positions/normals/UVs, triangle index order, map sections and NoData omission.
- Renderer source and Metal shaders compile. Camera projection checks covered 192 model corners over six portrait/landscape viewport shapes.
- Final horizon mesh helpers passed 18 temporary Metal-buffer geometry scenarios plus cancellation. Cases cover 1 m → 8 m → 32 m joins, 16 m primary → 8 m near, far directly to primary, fractional crops, large coordinate offsets, touching coverage edges and NoData. Checks include complete ring coverage, no inner overlap, boundary coordinate/height agreement, shared world frame, index bounds, buffer lengths and positive triangle winding.
- Horizon preparation checked all 196 prepared 8 m parents, 364 shared seams, six matching OSM archives and 64 geographic sample comparisons. Missing-source and NoData-preservation checks passed. All 27 existing primary terrain, map and graph assets remained byte-identical. Provenance is in `HORIZON_PROVENANCE.md`.
- Consistent-cartography update: all 196 near maps and 196 far maps pass complete coverage, image dimensions and hashes. Every far map exactly matches its filtered near source; all 16 parents overlapping primary detail maps exactly match their filtered masters. Full feature selection includes 4,629 footpaths, 1,999 tracks and 161 bridleways, plus the shared contour and map styling. All existing terrain and primary files remain unchanged.
- Updated renderer passed 21 geometry cases, including 196 near and 196 far tiles, nonuniform latitude boundaries, fractional origins, exact texture containment, seam heights, NoData, index bounds and cancellation. Terrain resolution boundaries share one material; each PNG upload is bounded by an autorelease pool. Texture-only metadata changes invalidate the rendered scene.
- Earlier band-map selection preview cached source/selection validation independently of device admission. An optimized host probe measured approximately 15 ms for a complete tiled preview and 0.02–0.06 ms for all six allowances from that preview; these are development measurements, not iPhone frame-rate results.
- Earlier band-map source preparation: original cell (0,0) at 1 m keeps near 8 m/far 32 m with 16/121 map tiles, 504.15 MiB estimated scene memory and 47.98 MiB saved assets. A 4 × 4 selection starting at (0,0) keeps both bands with 25/144 maps, 1070.28 MiB estimated memory and 76.42 MiB saved assets. Admission limits and the conservative texture allocation estimate are unchanged. These are estimates and measured disk sizes, not observed device peak memory.
- Property lists and `git diff --check` passed.

## Simulator interactions checked

14 September follow-up: verified a fresh selection defaults to 4 m with Auto off, and selecting all 256 cells retains 4 m with Save available. Saved and opened the full Eryri collection in the optimized Debug simulator build. The 2049² primary grid is genuine 4 m; both 16 m near and 32 m far horizons are present, with 3,136 map cells. Actual saved size is 721,444,984 bytes. The perspective view, labels, continuous map and overhead control render successfully. Screenshot: `Tools/reports/QA/full-collection-4m.png`; saved metadata summary: `Tools/reports/load-performance-2026-09-14/simulator-full-4m.json`. This resolves the earlier locked-Mac visual check. M1 iPad Pro loading and frame-rate measurements remain outstanding.

Earlier interaction records follow:

- Extension flow: started a direct-line route in one original 1 m cell and tapped surrounding terrain. The first waypoint remained in place while a blue pending pin and a 3 × 3 rectangle preview showed **8 additional tiles** and **56.8 MB** estimated saved size. Choosing 2 m showed an explicit whole-area **1 m → 2 m** change beside Save. Switching back to 1 m and saving opened the larger area with the same overhead viewpoint and completed the second waypoint; the resulting route measured approximately **610 m**. The current cartography continued across the enlarged scene. Preview: `Tools/reports/QA/extend-area-preview.png`.
- Reopened that extended route after relaunch, continued editing and checked the narrow landscape toolbar: Done remains on one line with a second row when needed. The saved route and terrain survived relaunch. The temporary QA route was deleted after checking. Screenshot: `Tools/reports/QA/extended-area-route.png`.

- Earlier band-map cartography build: re-saved original tile `c04-02 · p03-03` at 1 m, opened in 3D and visually verified contours, paths and the same map palette continue across the selected area and surrounding hills with the horizon fade intact. Screenshot: `Tools/reports/QA/consistent-cartography-single-1m.png`. This screenshot predates the independent atlas renderer and still has the image-density discontinuities that update removes.

- Dynamic allowance: Auto selects native 1 m for a 4 × 4 group and 2 m for a 5 × 5 group under the bounded simulator profile. Manual 4 m remains selected when the area changes; tapping Auto restores the finest fitting resolution.
- Saved and opened a 4 × 4 native 1 m Eryri area, then exercised overhead and fit camera controls. The installed grid is 2049 × 2049 (4,198,401 samples), with 16,777,216 total map pixels across its cropped textures. Screenshots: `Tools/reports/QA/automatic-detail-4x4-1m.png` and `Tools/reports/QA/terrain-4x4-1m.png`.
- Saved the same 4 × 4 selection again with the new surroundings. Primary grid remains 2049² at 1 m; near terrain is 781 × 783 at 8 m and far terrain 721 × 720 at 32 m. The actual installed pack is 22,943,484 bytes, including both context maps. The first integrated 3D check showed continuous terrain without the old slab walls.
- Final camera check: both the 4 × 4 area and a single native 1 m precision cell open with visible distant ridgelines and atmospheric fade. Overhead and fit controls work. A tap on surrounding terrain did not add a route point; a tap within the selected area did. The temporary test route was deleted. Screenshots: `Tools/reports/QA/horizon-4x4-1m.png` and `Tools/reports/QA/horizon-single-1m.png`.

- iPhone 17 Pro: bundled UK atlas, original Eryri 16 × 16 grid, marker-to-grid zoom, direct atlas-cell selection, single-tile selection, two-corner rectangles, pinned resolution/save controls, saved-area library and persisted route library.
- Current tile workflow: selected original cell `z10-x500-y333-c04-00-p02-03`, saved genuine 1 m and opened its 3D terrain. Inspected the installed pack: 513² heightfield, 1024² map, exact original grid identity; actual complete pack 1,059,322 bytes. The displayed pre-save size is a conservative estimate.
- Final selection-state checks: a saved-area shortcut appears only for the selected installed tile and disappears on selecting an unsaved neighbor; cell tapping still works after restricting map pan to zoomed previews.
- The earlier fixed-budget selector screenshot is retained at `Tools/reports/QA/tile-selector-1m.png`; the previous universal 1.1-million-sample cap has been replaced by the dynamic device allowance.
- Planned a real route near Yr Wyddfa and Crib Goch using the walking graph; undo, redo and reopen worked.
- Exported that route with the actual iOS Files exporter, then imported it through the Files picker. Its 243 points, two waypoints, elevations, name and segment survived the codec roundtrip. Measured distance 2,657.86 m; ascent 129.59 m; descent 329.27 m.
- Selected the central quarter of Snowdon, installed 4 m terrain and opened it in 3D. Saved grid 769 × 769; actual complete pack 5,825,380 bytes. Two-corner map selection also updated bounds and made 4 m available.
- Fit, zoom and phone rotation controls worked. The terrain has closed walls/base, and the landscape planner keeps a separate scrollable route panel.
- iPad mini: atlas, area preview, all six summit resolutions available, genuine 1 m summit save/open and 3D rendering worked.
- Local package picker now opens with folder/package selection after consolidating the root file-import presentation. The host Mac locked before the final Files Browse → package selection could complete.

## Remaining device and distribution checks

Simulator checks do not establish physical-device memory, gesture feel, frame rate, power consumption or thermal behavior. Test those on the oldest supported phone, including repeated area replacement and multitouch orbit/pan/pinch. The automated desktop drag did not exercise simulator touch dragging reliably; actual taps, route placement, camera buttons and accessibility area actions were exercised.

The final local-package Files selection/security-scope lifecycle, device signing and real-network interrupted downloads and retries need physical-device checks. The static HTTPS downloader is implemented, but no public pack host or national download catalog is deployed. The main shipped collection contains 256 original Eryri precision cells with all six terrain resolutions and a broader 27.4 km context source; three earlier Welsh prototype areas remain for compatibility. Source coverage cells describe prepared data, not an automatically published UK download service. The approximately 1.29 GB bundle includes source levels, independent atlas imagery and legacy maps; new atlas scenes install only selected terrain, covering atlas cells and admitted surrounding bands.

No App Store upload, signing change or external publication was performed. Original terrain/source projects and the external drive were read-only inputs.
